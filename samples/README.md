Add one or more test invoice PDFs in this folder.

Expected examples:
- `sample_invoice_1.pdf`
- `sample_invoice_2.pdf`

These files are used for local manual testing with:
`POST /api/invoices/extract`
